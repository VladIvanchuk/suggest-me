import s from "./Body.module.scss";

const Body = () => {
    return (
        <div className={s.body}>
            <h1 className={s.title}>Suggest.me</h1>
        </div>
    );
};

export default Body;
